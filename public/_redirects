# These rules will change if you change your site's custom domains or HTTPS settings

# Redirect default Netlify subdomain to primary domain
https://rockymtnrambler.netlify.com/* https://rockymtnrambler.com/:splat 301!

# Social media crawlers prerender
/* /:splat 200! Role=bot,crawler

# SPA fallback
/* /index.html 200